
API allows customize epilogue and prologue, or add to epilogue/prologue
allows specify module name to load
allow specify command to run between ''logues
meaningfully name task (?)
meaningful defaults (nodes etc)
specify CPU or GPU
specify interactive or non-interactive
logs command maybe
autofill name, email, walltime



use json

[prompt select job type]
prompt for inputfile paths (and node options)
cd to working directory -> copy necessary files (if needed)


get_opts or defaults (dict) -> request command (create_command) -> submit job
could use a config.py file or config.yaml instead of os.environ
